# === CORE API ===
fastapi==0.115.0
uvicorn==0.30.0
pydantic==2.9.0
email-validator==2.3.0
python-multipart==0.0.9

# === DATABASE ===
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.1

# === AUTHENTICATION ===
python-jose==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
cryptography==46.0.3

# === MACHINE LEARNING ===
tensorflow==2.16.1
numpy==1.26.4
pillow==10.4.0
keras==3.13.1

# === SECURITY & RATE LIMITING ===
slowapi==0.1.9
python-magic==0.4.27

# === AI/LLM (CHATBOT) ===
groq==0.11.0
python-dotenv==1.0.1

# === RAG PIPELINE ===
# chromadb: persistent vector store. Uses SQLite internally — no server needed.
# Pin to 0.5.x for stability; 0.6+ has breaking API changes.
chromadb==0.5.23
# sentence-transformers: loads all-MiniLM-L6-v2 (~80MB) for embeddings.
# torch is a transitive dependency — CPU-only build is sufficient.
sentence-transformers==3.3.1
# tokenizers is required by sentence-transformers
tokenizers>=0.19,<0.22
tf-keras==2.16.0

# === TESTING ===
pytest==8.3.0
httpx==0.27.0

# === OPTIONAL ===
matplotlib==3.9.0
rich==14.3.1
requests==2.32.5